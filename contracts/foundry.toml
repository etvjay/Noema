[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
cache_path = "cache"
libs = ["lib"]
solc_version = "0.8.36"
optimizer = true
optimizer_runs = 200

remappings = [
  "forge-std/=lib/forge-std/src/"
]
